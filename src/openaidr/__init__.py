"""OpenAIDR — session collection for AI coding agents.

Reads the session state agents already write to disk and normalises it into one
session model. Plumbing, not judgement: no scoring, no identity resolution, no
findings, no upload path.
"""

__version__ = "0.1.1"
